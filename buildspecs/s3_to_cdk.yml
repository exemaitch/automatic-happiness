version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"
  #secrets-manager:
     # key: secret-id:json-key:version-stage:version-id
     # key: secret-id:json-key:version-stage:version-id
  #exported-variables:
     # - variable
     # - variable
  #git-credential-helper: yes

phases:
  install:
    #If you use the Ubuntu standard image 2.0 or later, you must specify runtime-versions.
    #If you specify runtime-versions and use an image other than Ubuntu standard image 2.0, the build fails.
    runtime-versions:
      nodejs: 10
      python: 3.7
    commands:
      - npm install -g cdk
      - pip install awscli
      - pip install -r requirements.txt
  pre_build:
    commands:
      - pwd
      - ls -latr
      - mkdir build
  build:
    commands:
      - cdk synth > build/template.yml
      # - command
  #post_build:
    #commands:
      # - command
      # - command
  #reports:
    #enter-report-name-here:
      #files:
        # - location
        # - location
      #base-directory: location
      #discard-paths: yes
      #file-format: JunitXml | CucumberJson
artifacts:
  files:
    - template.yml
    # - location
  name: $(date +%Y-%m-%d)-template.yml
  #discard-paths: yes
  base-directory: build
#cache:
  #paths:
    # - paths